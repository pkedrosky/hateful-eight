[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_functions = ["test_*"]
addopts = "-v --tb=short"

[project]
name = "hateful-eight"
version = "0.1.0"
requires-python = ">=3.10"
